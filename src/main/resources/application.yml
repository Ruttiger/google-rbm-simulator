auth:
  mode: PERMISSIVE # o STRICT
  issuer: "https://auth.simulador.local"
  audience: "rbm.googleapis.com"
  tokenTtlSeconds: 3600
  acceptAnyClientInPermissive: true
  acceptedClients:
    - clientId: test-client
      clientSecret: secret
  allowedScopes: [ "rbm.bots", "rbm.messages" ]
  key:
    generateOnStartup: true
    keyId: "sim-key-1"
    privateKeyPem: "" # opcional si no generas
    publicKeyPem: ""  # opcional si no generas
server:
  port: 8080

management:
  tracing:
    enabled: false
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: http://otel-collector:4317
      export:
        enabled: false

logbook:
  filter:
    enabled: true
  format:
    style: json
  obfuscate:
    headers: [ Authorization ]
    parameters: [ access_token, password ]
  # Registra los cuerpos de las peticiones y respuestas cuando es true
  log-body: true
  # Máximo de caracteres a mostrar del body cuando log-body está activo
  max-body-size: 2048

logging:
  level:
    org.zalando.logbook: TRACE
